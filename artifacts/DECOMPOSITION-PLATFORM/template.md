# Decomposition: {Platform Name}

> **Level**: L0 (Platform / Host App)

## Table of Contents

<!-- generated by `cfs toc` -->

## 1. Overview

{How the Platform DESIGN was decomposed into MiniApps, the decomposition strategy, and the rationale. Each MiniApp is a self-contained module that can be developed, released, and activated independently.}

**Parent Documents:**
- PRD: [PRD.md](./PRD.md)
- DESIGN: [DESIGN.md](./DESIGN.md)

## 2. Entries

**Overall implementation status:**

- [ ] `p1` - **ID**: `cpt-{hierarchy-prefix}-status-overall`

---

### 2.1 [{MiniApp Name 1}](miniapps/{miniapp1}/) - HIGH

- [ ] `p1` - **ID**: `cpt-{hierarchy-prefix}-miniapp-{miniapp1}`

- **Purpose**: {Few sentences describing what this MiniApp provides and why it exists}

- **Target Users**: `cpt-{hierarchy-prefix}-actor-{slug}`

- **Depends On**: None (kernel only)

- **Scope**:
  - {in-scope capability 1}
  - {in-scope capability 2}

- **Out of scope**:
  - {out-of-scope item 1}

- **Requirements Covered**:

  - [ ] `p1` - `cpt-{hierarchy-prefix}-fr-{slug}` — {requirement summary}
  - [ ] `p1` - `cpt-{hierarchy-prefix}-nfr-{slug}` — {requirement summary}

- **Design Principles Covered**:

  - [ ] `p1` - `cpt-{hierarchy-prefix}-principle-{slug}`

- **Design Constraints Covered**:

  - [ ] `p1` - `cpt-{hierarchy-prefix}-constraint-{slug}`

- **Domain Model Entities**:
  - {shared entity 1}
  - {shared entity 2}

- **Design Components**:

  - [ ] `p1` - `cpt-{hierarchy-prefix}-component-{slug}` — {platform component this MiniApp consumes or extends}

- **Kernel Contracts Consumed**:

  - [ ] `p1` - `cpt-{hierarchy-prefix}-contract-{slug}` — {auth / storage / network / notifications}

- **Deep Link Namespace**: `{scheme}://{miniapp1}/*`

- **Sequences**:

  - [ ] `p1` - `cpt-{hierarchy-prefix}-seq-{slug}`

- **Data**:

  - [ ] `p1` - `cpt-{hierarchy-prefix}-db-{slug}`

- **Integration Points**:
  - `cpt-{hierarchy-prefix}-integration-{slug}` — {external system}

- **Target Release**: Q{X} 202{Y}

---

### 2.2 [{MiniApp Name 2}](miniapps/{miniapp2}/) - MEDIUM

- [ ] `p2` - **ID**: `cpt-{hierarchy-prefix}-miniapp-{miniapp2}`

- **Purpose**: {Few sentences}

- **Target Users**: `cpt-{hierarchy-prefix}-actor-{slug}`

- **Depends On**: `cpt-{hierarchy-prefix}-miniapp-{miniapp1}` — {why}

- **Scope**:
  - {in-scope capability}

- **Out of scope**:
  - {out-of-scope item}

- **Requirements Covered**:

  - [ ] `p2` - `cpt-{hierarchy-prefix}-fr-{slug}` — {requirement summary}

- **Design Principles Covered**:

  - [ ] `p2` - `cpt-{hierarchy-prefix}-principle-{slug}`

- **Design Constraints Covered**:

  - [ ] `p2` - `cpt-{hierarchy-prefix}-constraint-{slug}`

- **Domain Model Entities**:
  - {shared entity}

- **Design Components**:

  - [ ] `p2` - `cpt-{hierarchy-prefix}-component-{slug}`

- **Kernel Contracts Consumed**:

  - [ ] `p2` - `cpt-{hierarchy-prefix}-contract-{slug}`

- **Deep Link Namespace**: `{scheme}://{miniapp2}/*`

- **Sequences**:

  - [ ] `p2` - `cpt-{hierarchy-prefix}-seq-{slug}`

- **Data**:

  - [ ] `p2` - `cpt-{hierarchy-prefix}-db-{slug}`

- **Integration Points**:
  - `cpt-{hierarchy-prefix}-integration-{slug}`

- **Target Release**: Q{X} 202{Y}

---

### 2.3 [{MiniApp Name 3}](miniapps/{miniapp3}/) - LOW

- [ ] `p3` - **ID**: `cpt-{hierarchy-prefix}-miniapp-{miniapp3}`

- **Purpose**: {Few sentences}

- **Target Users**: `cpt-{hierarchy-prefix}-actor-{slug}`

- **Depends On**: `cpt-{hierarchy-prefix}-miniapp-{miniapp1}`

- **Scope**:
  - {in-scope capability}

- **Out of scope**:
  - {out-of-scope item}

- **Requirements Covered**:

  - [ ] `p3` - `cpt-{hierarchy-prefix}-fr-{slug}`

- **Design Principles Covered**:

  - [ ] `p3` - `cpt-{hierarchy-prefix}-principle-{slug}`

- **Design Constraints Covered**:

  - [ ] `p3` - `cpt-{hierarchy-prefix}-constraint-{slug}`

- **Domain Model Entities**:
  - {shared entity}

- **Design Components**:

  - [ ] `p3` - `cpt-{hierarchy-prefix}-component-{slug}`

- **Kernel Contracts Consumed**:

  - [ ] `p3` - `cpt-{hierarchy-prefix}-contract-{slug}`

- **Deep Link Namespace**: `{scheme}://{miniapp3}/*`

- **Sequences**:

  - [ ] `p3` - `cpt-{hierarchy-prefix}-seq-{slug}`

- **Data**:

  - [ ] `p3` - `cpt-{hierarchy-prefix}-db-{slug}`

- **Integration Points**:
  - `cpt-{hierarchy-prefix}-integration-{slug}`

- **Target Release**: Q{X} 202{Y}

---

## 3. Entry Dependencies

```text
cpt-{hierarchy-prefix}-miniapp-{miniapp1}
    ↓
    ├─→ cpt-{hierarchy-prefix}-miniapp-{miniapp2}
    └─→ cpt-{hierarchy-prefix}-miniapp-{miniapp3}
```

**Dependency Rationale**:

- `cpt-{hierarchy-prefix}-miniapp-{miniapp2}` requires `cpt-{hierarchy-prefix}-miniapp-{miniapp1}`: {why}
- `cpt-{hierarchy-prefix}-miniapp-{miniapp3}` requires `cpt-{hierarchy-prefix}-miniapp-{miniapp1}`: {why}
- `{miniapp2}` and `{miniapp3}` are independent of each other and can be developed in parallel

## 4. Shared Kernel Coverage

| Component ID | Component | MiniApps Using |
|--------------|-----------|----------------|
| `cpt-{hierarchy-prefix}-component-auth-kernel` | Authentication | All |
| `cpt-{hierarchy-prefix}-component-storage-kernel` | Storage | All |
| `cpt-{hierarchy-prefix}-component-network-kernel` | Networking | All |
| `cpt-{hierarchy-prefix}-component-notifications-kernel` | Notifications | {list} |

## 5. Coverage Matrix

### 5.1 Requirements Coverage

| Requirement ID | Covering Entry | Status |
|----------------|----------------|--------|
| `cpt-{hierarchy-prefix}-fr-{slug}` | `cpt-{hierarchy-prefix}-miniapp-{slug}` | ☐ |
| `cpt-{hierarchy-prefix}-nfr-{slug}` | {kernel component or all MiniApps} | ☐ |

### 5.2 Design Component Coverage

| Component ID | Covering Entry | Status |
|--------------|----------------|--------|
| `cpt-{hierarchy-prefix}-component-{slug}` | `cpt-{hierarchy-prefix}-miniapp-{slug}` | ☐ |

## 6. Release Roadmap

| Quarter | MiniApps | Milestone |
|---------|----------|-----------|
| Q{X} 202{Y} | {MiniApp (MVP)} | {Milestone} |
| Q{X} 202{Y} | {MiniApps} | {Milestone} |
