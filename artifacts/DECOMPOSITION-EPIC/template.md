# Decomposition: {Epic Name}

> **Level**: L2 (Epic)

## Table of Contents

<!-- generated by `cfs toc` -->

## 1. Overview

{How the Epic DESIGN was decomposed into features, the decomposition strategy, and the rationale. Each feature is a discrete piece of functionality that can be implemented, tested, and delivered independently.}

**Parent Documents:**
- Epic PRD: [PRD.md](./PRD.md)
- Epic DESIGN: [DESIGN.md](./DESIGN.md)
- MiniApp DECOMPOSITION: [../DECOMPOSITION.md](../DECOMPOSITION.md)

## 2. Entries

**Overall implementation status:**

- [ ] `p1` - **ID**: `cpt-{hierarchy-prefix}-status-overall`

---

### 2.1 [{Feature Name 1}](features/{feature1}/) - HIGH

- [ ] `p1` - **ID**: `cpt-{hierarchy-prefix}-feature-{feature1}`

- **Purpose**: {Few sentences describing what this feature accomplishes and why it matters}

- **Depends On**: None

- **Scope**:
  - {in-scope item 1}
  - {in-scope item 2}

- **Out of scope**:
  - {out-of-scope item}

- **Requirements Covered**:

  - [ ] `p1` - `cpt-{hierarchy-prefix}-fr-{slug}` — {requirement summary}
  - [ ] `p1` - `cpt-{hierarchy-prefix}-state-{slug}` — {state requirement}

- **Design Principles Covered**:

  - [ ] `p1` - `cpt-{hierarchy-prefix}-principle-{slug}`

- **Design Constraints Covered**:

  - [ ] `p1` - `cpt-{hierarchy-prefix}-constraint-{slug}`

- **Domain Model Entities**:
  - {entity 1}

- **Design Components**:

  - [ ] `p1` - `cpt-{hierarchy-prefix}-component-{slug}` — {component from Epic DESIGN}
  - [ ] `p1` - `cpt-{hierarchy-prefix}-widget-{slug}` — {widget}

- **Use Cases**:

  - [ ] `p1` - `cpt-{hierarchy-prefix}-usecase-{slug}`

- **API**:
  - GET /api/v1/mobile/{miniapp}/{resource}
  - POST /api/v1/mobile/{miniapp}/{resource}

- **Sequences**:

  - [ ] `p1` - `cpt-{hierarchy-prefix}-seq-{slug}`

- **Data**:

  - [ ] `p1` - `cpt-{hierarchy-prefix}-db-{slug}`

- **Platform Implementation**:

| Platform | Module | Location |
|----------|--------|----------|
| Shared | `{Feature}ViewModel`, `{Feature}UseCase` | `constructor-sdk/feature/{miniapp}/` |
| Android | `{Feature}Screen` | `android-app/feature/{miniapp}/ui/` |
| iOS | `{Feature}View` | `ios-app/Features/{MiniApp}/Views/` |

---

### 2.2 [{Feature Name 2}](features/{feature2}/) - MEDIUM

- [ ] `p2` - **ID**: `cpt-{hierarchy-prefix}-feature-{feature2}`

- **Purpose**: {Few sentences}

- **Depends On**: `cpt-{hierarchy-prefix}-feature-{feature1}` — {why}

- **Scope**:
  - {in-scope item}

- **Out of scope**:
  - {out-of-scope item}

- **Requirements Covered**:

  - [ ] `p2` - `cpt-{hierarchy-prefix}-fr-{slug}` — {requirement summary}

- **Design Principles Covered**:

  - [ ] `p2` - `cpt-{hierarchy-prefix}-principle-{slug}`

- **Design Constraints Covered**:

  - [ ] `p2` - `cpt-{hierarchy-prefix}-constraint-{slug}`

- **Domain Model Entities**:
  - {entity}

- **Design Components**:

  - [ ] `p2` - `cpt-{hierarchy-prefix}-widget-{slug}`

- **Use Cases**:

  - [ ] `p2` - `cpt-{hierarchy-prefix}-usecase-{slug}`

- **API**:
  - PUT /api/v1/mobile/{miniapp}/{resource}/{id}

- **Sequences**:

  - [ ] `p2` - `cpt-{hierarchy-prefix}-seq-{slug}`

- **Data**:

  - [ ] `p2` - `cpt-{hierarchy-prefix}-db-{slug}`

- **Platform Implementation**:

| Platform | Module | Location |
|----------|--------|----------|
| Shared | `{Module}` | `constructor-sdk/feature/{miniapp}/` |
| Android | `{Component}` | `android-app/feature/{miniapp}/` |
| iOS | `{Component}` | `ios-app/Features/{MiniApp}/` |

---

### 2.3 [{Feature Name 3}](features/{feature3}/) - LOW

- [ ] `p3` - **ID**: `cpt-{hierarchy-prefix}-feature-{feature3}`

- **Purpose**: {Few sentences}

- **Depends On**: `cpt-{hierarchy-prefix}-feature-{feature1}`

- **Scope**:
  - {in-scope item}

- **Out of scope**:
  - {out-of-scope item}

- **Requirements Covered**:

  - [ ] `p3` - `cpt-{hierarchy-prefix}-fr-{slug}`

- **Design Principles Covered**:

  - [ ] `p3` - `cpt-{hierarchy-prefix}-principle-{slug}`

- **Design Constraints Covered**:

  - [ ] `p3` - `cpt-{hierarchy-prefix}-constraint-{slug}`

- **Domain Model Entities**:
  - {entity}

- **Design Components**:

  - [ ] `p3` - `cpt-{hierarchy-prefix}-widget-{slug}`

- **Use Cases**:

  - [ ] `p3` - `cpt-{hierarchy-prefix}-usecase-{slug}`

- **API**:
  - {endpoint}

- **Sequences**:

  - [ ] `p3` - `cpt-{hierarchy-prefix}-seq-{slug}`

- **Data**:

  - [ ] `p3` - `cpt-{hierarchy-prefix}-db-{slug}`

- **Platform Implementation**:

| Platform | Module | Location |
|----------|--------|----------|
| Shared | `{Module}` | `constructor-sdk/feature/{miniapp}/` |
| Android | `{Component}` | `android-app/feature/{miniapp}/` |
| iOS | `{Component}` | `ios-app/Features/{MiniApp}/` |

---

## 3. Feature Dependencies

```text
cpt-{hierarchy-prefix}-feature-{feature1}
    ↓
    ├─→ cpt-{hierarchy-prefix}-feature-{feature2}
    └─→ cpt-{hierarchy-prefix}-feature-{feature3}
```

**Dependency Rationale**:

- `cpt-{hierarchy-prefix}-feature-{feature2}` requires `cpt-{hierarchy-prefix}-feature-{feature1}`: {why}
- `cpt-{hierarchy-prefix}-feature-{feature3}` requires `cpt-{hierarchy-prefix}-feature-{feature1}`: {why}
- `{feature2}` and `{feature3}` are independent of each other and can be developed in parallel

## 4. Coverage Matrix

### 4.1 Requirements Coverage

| Requirement ID | Covering Feature | Priority | Status |
|----------------|------------------|----------|--------|
| `cpt-{hierarchy-prefix}-fr-{slug}` | `cpt-{hierarchy-prefix}-feature-{feature1}` | p1 | ☐ |
| `cpt-{hierarchy-prefix}-state-{slug}` | `cpt-{hierarchy-prefix}-feature-{feature1}` | p1 | ☐ |

### 4.2 Design Component Coverage

| Component ID | Covering Feature | Status |
|--------------|------------------|--------|
| `cpt-{hierarchy-prefix}-component-{slug}` | `cpt-{hierarchy-prefix}-feature-{feature1}` | ☐ |
| `cpt-{hierarchy-prefix}-widget-{slug}` | `cpt-{hierarchy-prefix}-feature-{feature2}` | ☐ |
| `cpt-{hierarchy-prefix}-usecase-{slug}` | `cpt-{hierarchy-prefix}-feature-{feature1}` | ☐ |

### 4.3 Platform Implementation Matrix

| Feature | Shared | Android | iOS |
|---------|--------|---------|-----|
| `cpt-{hierarchy-prefix}-feature-{feature1}` | ☐ | ☐ | ☐ |
| `cpt-{hierarchy-prefix}-feature-{feature2}` | ☐ | ☐ | ☐ |
| `cpt-{hierarchy-prefix}-feature-{feature3}` | ☐ | ☐ | ☐ |

## 5. Implementation Order

| Phase | Features | Rationale |
|-------|----------|-----------|
| 1 | `cpt-{hierarchy-prefix}-feature-{feature1}` | Foundation, no dependencies |
| 2 | `cpt-{hierarchy-prefix}-feature-{feature2}` | Depends on phase 1 |
| 3 | `cpt-{hierarchy-prefix}-feature-{feature3}` | Can be parallel after phase 1 |

## 6. Acceptance Criteria Summary

| Feature | Key Acceptance Criteria |
|---------|-------------------------|
| `cpt-{hierarchy-prefix}-feature-{feature1}` | {1-2 key criteria} |
| `cpt-{hierarchy-prefix}-feature-{feature2}` | {1-2 key criteria} |
