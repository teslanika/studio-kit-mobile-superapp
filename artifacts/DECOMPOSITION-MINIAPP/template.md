# Decomposition: {MiniApp Name} MiniApp

> **Level**: L1 (MiniApp)

## Table of Contents

<!-- generated by `cfs toc` -->

## 1. Overview

{How the MiniApp DESIGN was decomposed into epics, the decomposition strategy, and the rationale. Each epic is a screen, capability, or flow that can be developed and tested independently.}

**Parent Documents:**
- MiniApp PRD: [PRD.md](./PRD.md)
- MiniApp DESIGN: [DESIGN.md](./DESIGN.md)
- Platform DECOMPOSITION: [../DECOMPOSITION.md](../DECOMPOSITION.md)

## 2. Entries

**Overall implementation status:**

- [ ] `p1` - **ID**: `cpt-{hierarchy-prefix}-status-overall`

---

### 2.1 [{Screen Name 1}](screens/{screen1}/) - HIGH

- [ ] `p1` - **ID**: `cpt-{hierarchy-prefix}-epic-{screen1}`

- **Category**: Screen

- **Purpose**: {Few sentences describing what this epic provides and why it matters}

- **Actors**: `cpt-superapp-actor-{slug}`

- **Depends On**: None (entry point)

- **Scope**:
  - {in-scope item 1}
  - {in-scope item 2}

- **Out of scope**:
  - {out-of-scope item}

- **Requirements Covered**:

  - [ ] `p1` - `cpt-{hierarchy-prefix}-fr-{slug}` — {requirement summary}
  - [ ] `p1` - `cpt-{hierarchy-prefix}-nfr-{slug}` — {requirement summary}

- **Design Principles Covered**:

  - [ ] `p1` - `cpt-{hierarchy-prefix}-principle-{slug}`

- **Design Constraints Covered**:

  - [ ] `p1` - `cpt-{hierarchy-prefix}-constraint-{slug}`

- **Domain Model Entities**:
  - {entity 1}
  - {entity 2}

- **Design Components**:

  - [ ] `p1` - `cpt-{hierarchy-prefix}-component-{slug}` — {component from MiniApp DESIGN}

- **API**:
  - GET /api/v1/mobile/{miniapp}/{resource}
  - POST /api/v1/mobile/{miniapp}/{resource}

- **Sequences**:

  - [ ] `p1` - `cpt-{hierarchy-prefix}-seq-{slug}`

- **Data**:

  - [ ] `p1` - `cpt-{hierarchy-prefix}-db-{slug}`
  - [ ] `p1` - `cpt-{hierarchy-prefix}-dbtable-{slug}`

- **Entry Points**: {navigation | deep link `{scheme}://{miniapp}/{path}` | notification}

- **Platform Coverage**: Shared ☐ / Android ☐ / iOS ☐

- **Target Release**: Q{X} 202{Y}

---

### 2.2 [{Capability Name}](capabilities/{capability}/) - MEDIUM

- [ ] `p2` - **ID**: `cpt-{hierarchy-prefix}-epic-{capability}`

- **Category**: Capability

- **Purpose**: {Few sentences}

- **Actors**: `cpt-superapp-actor-{slug}`

- **Depends On**: `cpt-{hierarchy-prefix}-epic-{screen1}` — {why}

- **Scope**:
  - {in-scope item}

- **Out of scope**:
  - {out-of-scope item}

- **Requirements Covered**:

  - [ ] `p2` - `cpt-{hierarchy-prefix}-fr-{slug}` — {requirement summary}

- **Design Principles Covered**:

  - [ ] `p2` - `cpt-{hierarchy-prefix}-principle-{slug}`

- **Design Constraints Covered**:

  - [ ] `p2` - `cpt-{hierarchy-prefix}-constraint-{slug}`

- **Domain Model Entities**:
  - {entity}

- **Design Components**:

  - [ ] `p2` - `cpt-{hierarchy-prefix}-component-{slug}`

- **API**:
  - PUT /api/v1/mobile/{miniapp}/{resource}/{id}

- **Sequences**:

  - [ ] `p2` - `cpt-{hierarchy-prefix}-seq-{slug}`

- **Data**:

  - [ ] `p2` - `cpt-{hierarchy-prefix}-db-{slug}`

- **Entry Points**: {how this capability is reached or invoked}

- **Platform Coverage**: Shared ☐ / Android ☐ / iOS ☐

- **Target Release**: Q{X} 202{Y}

---

### 2.3 [{Flow Name}](flows/{flow}/) - LOW

- [ ] `p3` - **ID**: `cpt-{hierarchy-prefix}-epic-{flow}`

- **Category**: Flow

- **Purpose**: {Few sentences}

- **Actors**: `cpt-superapp-actor-{slug}`

- **Depends On**: `cpt-{hierarchy-prefix}-epic-{screen1}`, `cpt-{hierarchy-prefix}-epic-{capability}`

- **Scope**:
  - {in-scope item}

- **Out of scope**:
  - {out-of-scope item}

- **Requirements Covered**:

  - [ ] `p3` - `cpt-{hierarchy-prefix}-fr-{slug}`

- **Design Principles Covered**:

  - [ ] `p3` - `cpt-{hierarchy-prefix}-principle-{slug}`

- **Design Constraints Covered**:

  - [ ] `p3` - `cpt-{hierarchy-prefix}-constraint-{slug}`

- **Domain Model Entities**:
  - {entity}

- **Design Components**:

  - [ ] `p3` - `cpt-{hierarchy-prefix}-component-{slug}`

- **API**:
  - {endpoint}

- **Sequences**:

  - [ ] `p3` - `cpt-{hierarchy-prefix}-seq-{slug}`

- **Data**:

  - [ ] `p3` - `cpt-{hierarchy-prefix}-db-{slug}`

- **Entry Points**: {entry into the flow and where it exits}

- **Platform Coverage**: Shared ☐ / Android ☐ / iOS ☐

- **Target Release**: Q{X} 202{Y}

---

## 3. Entry Dependencies

```text
cpt-{hierarchy-prefix}-epic-{screen1}
    ↓
    ├─→ cpt-{hierarchy-prefix}-epic-{capability}
    └─→ cpt-{hierarchy-prefix}-epic-{flow}
```

**Dependency Rationale**:

- `cpt-{hierarchy-prefix}-epic-{capability}` requires `cpt-{hierarchy-prefix}-epic-{screen1}`: {why}
- `cpt-{hierarchy-prefix}-epic-{flow}` requires both: {why}

## 4. Coverage Matrix

### 4.1 Requirements Coverage

| Requirement ID | Covering Entry | Status |
|----------------|----------------|--------|
| `cpt-{hierarchy-prefix}-fr-{slug}` | `cpt-{hierarchy-prefix}-epic-{slug}` | ☐ |
| `cpt-{hierarchy-prefix}-nfr-{slug}` | {epic or cross-cutting} | ☐ |

### 4.2 Design Component Coverage

| Component ID | Covering Entry | Status |
|--------------|----------------|--------|
| `cpt-{hierarchy-prefix}-component-{slug}` | `cpt-{hierarchy-prefix}-epic-{slug}` | ☐ |

## 5. Implementation Order

| Phase | Epics | Rationale |
|-------|-------|-----------|
| 1 | `cpt-{hierarchy-prefix}-epic-{screen1}` | Foundation, no dependencies |
| 2 | `cpt-{hierarchy-prefix}-epic-{capability}` | Cross-cutting, enables later epics |
| 3 | `cpt-{hierarchy-prefix}-epic-{flow}` | Requires screens and capability |
